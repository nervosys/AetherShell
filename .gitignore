# Build artifacts
target/
*.exe
*.pdb

# Infrastructure (private repo)
infra/

# Environment and secrets
.env
.env.*
# Agent-mode runtime artifacts: hash-chained audit log, transaction journals
# (backups), and any locally-created rbac config / secrets under the workspace.
.ae/

# Temporary files
temp/
*.tmp
*.log
*_test.ae
launch_test.*
security_audit_report.json

# Editor files
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Rust artifacts
Cargo.lock.bak

# Development artifacts
*.backup
*.orig
.test_output/
node_modules/
*.zip

# transient test artifacts
x.db
*.db

# Generated wasm-bindgen output, copied out of web/pkg by
# integrations/browser-extension/build.ps1 (which -Clean deletes wholesale).
# These were committed until 2026-07-30 and embedded the building developer's
# absolute paths — and so their username — in panic-location strings.
integrations/browser-extension/wasm/

# Python SDK build artifacts (integrations/python)
integrations/python/dist/
integrations/python/build/
integrations/python/**/*.egg-info/
