# Canonicalize commit identities to the project's organization address. Maps an
# earlier personal commit email to opensource@nervosys.ai so git tooling and
# GitHub display the org identity. (Does not rewrite history; raw commit objects
# are unchanged — full removal needs a history rewrite + force-push.)
nervosys <opensource@nervosys.ai> <adam.michael.erickson@gmail.com>
