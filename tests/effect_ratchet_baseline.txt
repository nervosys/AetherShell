# Builtins that construct a process, write a file, or open a socket while
# `safety::effect_of` returns `Effect::Pure`.
#
# This file may only shrink. It was introduced with 306 entries and emptied on
# 2026-08-11, when all 306 were classified from the argv their bodies build.
# A new builtin that acts while classified `Pure` fails
# `tests/effect_ratchet.rs` — classify it in `effect_of` rather than adding it
# here.
#
# Empty is the goal state, not a licence to refill.
