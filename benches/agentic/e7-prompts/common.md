You are writing a single shell command to answer one question about a dataset of
500 GitHub issues and pull requests from the `cli/cli` repository.

Rules:
- Output exactly one command, inside a single fenced code block.
- No prose, no explanation, no alternatives.
- The command runs with the data directory as the working directory.
- It must print only the answer.

Question: {{QUESTION}}
